# Git Policy Runbook

Do not run Git commands unless the user explicitly asks for Git workflow.

If Git is requested, keep commands non-destructive. Never run destructive Git
commands such as reset or checkout without explicit approval for that exact
operation.
